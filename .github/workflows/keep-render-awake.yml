name: Keep Render Awake

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render API
        run: curl -s -g -o /dev/null -w "%{http_code}" --max-time 30 'https://my-personal-blog-areb.onrender.com/api/posts?populate=*&sort[0]=publishedAt:desc'
